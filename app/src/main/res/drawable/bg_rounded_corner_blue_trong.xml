<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Thiết lập màu nền cho button -->
    <solid android:color="@color/blue_trong" /> <!-- Màu nền định sẵn -->
    <stroke
        android:width="2dp"
        android:color="@color/blue_trong" /> <!-- Màu của border -->
    <corners android:bottomLeftRadius="75dp" android:bottomRightRadius="75dp" /> <!-- Bán kính bo tròn -->

</shape>